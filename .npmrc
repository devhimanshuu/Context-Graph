# The pinned `react@19.1.0` does not satisfy @clerk/nextjs's peer range
# (`~19.1.4`). This project resolves peers leniently, matching how the
# lockfile was created; without this flag a fresh `npm install` fails with
# ERESOLVE.
legacy-peer-deps=true
